/*export interface Product {
    id: number;
    name: string;
    category: string;
    expireDate: string;
    inStock: boolean;
  }
*/
export interface Product {
  id: string;
  name: string;
  category: string;
  stock: boolean;
  expire: string ;
}